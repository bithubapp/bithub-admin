import Component from 'can-component';
import DefineMap from 'can-define/map/';
import template from './youtube-user.stache!';
import './youtube-user.less!';

export default Component.extend({
	tag : 'bh-youtube-user-service',
	template,
});
