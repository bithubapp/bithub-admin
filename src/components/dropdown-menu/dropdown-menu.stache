<div ($mouseenter)="toggle()" ($mouseleave)="toggle()">
	<span>{{buttonTitle}}</span>
	{{#if visible}}
		{{#if items.length}}
			<ul>
				{{#each items}}
					<li>{{title}}</li>
				{{/each}}
			</ul>
		{{/if}}
	{{/if}}
</div>
